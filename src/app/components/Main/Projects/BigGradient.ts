'use client'
import styled from 'styled-components'

export const BigGradient = styled.div`
  position: absolute;
  width: 642px;
  height: 720px;
  background: radial-gradient(
    50% 50% at 50% 50%,
    #763cac 0%,
    rgba(50, 15, 133, 0) 100%
  );
`
